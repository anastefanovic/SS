#pragma once
#ifndef _TYPEDEFS_H_
#define _TYPEDEFS_H_

#include <map>
#include <string>
#include <vector>
#include "relocation.h"


using RelocationTable = std::map<std::string, std::vector<Relocation>>;
using 
#endif